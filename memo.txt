kcl@kcl-XPS-13-9350:~$ history | grep apt
    4  sudo apt install pip3
    5  sudo apt install pip
    6  sudo apt-get install pip
    7  sudo apt-get install python-pip3
    8  sudo apt-get install python-pip
   10  sudo apt-get install python3-pip
   17  sudo apt update
   18  sudo apt install shadowsocks-libev
   20  sudo apt install yum
   23  sudo apt update
   24  sudo apt install git
   42  wget -qO - https://download.sublimetext.com/sublimehq-pub.gpg | sudo apt-key add -
   43  sudo apt-get install apt-transport-https
   44  echo "deb https://download.sublimetext.com/ apt/stable/" | sudo tee /etc/apt/sources.list.d/sublime-text.list
   45  sudo apt-get update
   46  sudo apt-get install sublime-text
   48  sudo apt-get install sublime-text
   49  sudo apt-get update
   50  sudo apt-get install sublime-text
   55  sudo apt-get install fcitx fcitx-chewing
   57  sudo apt install fcitx-googlepinyin
   58  sudo apt install fcitx
   62  sudo apt-get install -y chrome-gnome-shell
   63  sudo apt-get install ubuntu-gnome-desktop
   65  sudo apt install tasksel
   68  sudo apt-get install ubuntu-gnome-desktop
   69  sudo apt-get install ubuntu-gnome-desktop --no-install-recommends
   73  sudo apt-get install gnome-tweaks
   74  sudo apt-get install Plank-Preferences
   75  sudo apt-get install plank-preferences
   76  sudo apt-get install plank preferences
   77  sudo apt-get install plank
   80  sudo apt-get install plank --preference
   87  sudo apt install dconf
   88  sudo apt install dconf-editor
   89  sudo apt install dconf-tools
   93  sudo apt update
   94  sudo apt install kazam
  103  sudo apt install ./XMind-ZEN-for-Linux-64bit.deb
  105  sudo apt install -f ./XMind-ZEN-for-Linux-64bit.deb
  107  sudo apt install kdenlive
  108  sudo apt install virtualbox
  110  history | grep apt
  115  sudo apt-get install goldendict
  118  sudo add-apt-repository ppa:wiznote-team
  119  sudo apt-get update
  120  sudo apt-get install wiznote
  121  sudo apt install wiznote
  122  sudo apt install kazam
  123  sudo add-apt-repository ppa:wiznote-team
  124  sudo apt-get update
  125  sudo apt-get install wiznote
  126  sudo apt install openssl1.0 libssl1.0-dev 
  144  sudo apt-get install playonlinux
  146  sudo apt install dash-to-dock
  160  sudo apt update
  162  sudo apt install wine64
  163  history | grep apt


